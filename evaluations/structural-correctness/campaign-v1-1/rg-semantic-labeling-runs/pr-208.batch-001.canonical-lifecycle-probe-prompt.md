# PR 208 batch 001 — canonical lifecycle execution probe

Return exactly one JSON object with one field:

```json
{"status": "ok"}
```

Do not add Markdown, explanation, or additional keys. This is a transport and
execution-observability probe only. It contains no repository source,
candidate, proposal, retrieval, or assessment data.
